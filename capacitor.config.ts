import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.magic8ball.app',
  appName: '8ballApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
